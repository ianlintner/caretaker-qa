name: E2E Tests with KIND (Extended)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  e2e-kind-extended:
    uses: ./.github/workflows/reusable-kind-e2e.yml
    with:
      job_name: Extended E2E on KIND
      script_path: scripts/e2e_kind_extended.sh
      timeout_minutes: 40
